services:
  - type: web
    name: aegis-orchestrator
    env: python
    buildCommand: pip install -r projects/aegis/core/requirements.txt
    startCommand: uvicorn projects.aegis.core.cloud_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
